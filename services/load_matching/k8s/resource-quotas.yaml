apiVersion: v1
kind: ResourceQuota
metadata:
  name: load-matching-quota
spec:
  hard:
    requests.cpu: "2"
    requests.memory: 2Gi
    limits.cpu: "4"
    limits.memory: 4Gi
    pods: "10"
    services: "5"
    persistentvolumeclaims: "2"
    configmaps: "10"
    secrets: "10" 